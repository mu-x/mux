# ~/.muxrc to be included in ~/.bashrc
MUX=true

# Bash config
if [ $(basename $SHELL) == bash ]; then
    PS1='\n\[\033[01;32m\]\u@\D{%H%M%S}\[\033[00m\] \[\033[01;34m\]\w\[\033[00m\]\n\$ '
fi

# Maven debug aliases
alias mvn-compile-debug="mvn compile -Dbuild.configuration=debug"
alias mvn-package-debug="mvn package -Dbuild.configuration=debug"

# QtCreator silentl run
function sqtcreator() {
    CMD="qtcreator ${@:-$(echo *.pro)}"
    echo $CMD
    $CMD >/dev/null 2>&1 &
}

# Vim super developer mode
function svi() {
    sudo chmod +x $@
    sudo vi $@
}

# Start tmux or screen as main terminal emulator
TERMINAL_MULTIPLEXER=$(which tmux || whish screen)
if [[ $TERMINAL_MULTIPLEXER ]] && [[ $TERM != "screen" ]]; then
    exec $TERMINAL_MULTIPLEXER
fi
