Задание:

  Написать под Linux на C или С++ аналог стандартной команды find,
  но с ограниченными возможностями, а именно:

  - В параметре -name из шаблонных символов можно использовать только
    символ '?', означающий любой символ.

  - Остальные символы ищутся "как есть". Кроме -name обработку других
    параметров реализовывать не нужно.


Например:

  find /mydir -name 'a??.txt'


Вывод:

  /mydir/a01.txt
  /mydir/a05.txt
